pandas>=2.0
scikit-learn>=1.3
scipy>=1.10        # bounded scalar minimization for temperature-scaling fit
joblib>=1.3
onnxruntime>=1.16
skl2onnx>=1.16
numpy>=1.24

# Optional: widens system date-time parsing coverage in the NLU engine.
# The engine has a built-in pure-Python regex parser and works without this;
# install it for more robust natural-language date handling on the server side.
dateparser>=1.2 ; python_version >= "3.8"
