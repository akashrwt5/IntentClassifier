# Borderline de volume command asks first (ND-11a extension), never misfires.
lang: de
turns:
  - user: "mach es leiser"
    expect: {type: CONFIRM, intent: Cmd.VolumeDecrease}
  - user: "ja"
    expect: {type: FULFILL, intent: Cmd.VolumeDecrease}
