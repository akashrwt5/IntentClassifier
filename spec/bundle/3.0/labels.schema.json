{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spec.intent-platform.local/bundle/3.0/labels.schema.json",
  "title": "models/intent/{lang}/labels.json — ordered label list (the model output contract)",
  "description": "Order IS the contract: index i of the model's output maps to labels[i]. Must be identical (≡) to the compiled intent set per language — proved by compiler stage 8, the parity assert moved to compile time.",
  "type": "array",
  "minItems": 1,
  "uniqueItems": true,
  "items": { "$ref": "defs.schema.json#/$defs/intentId" }
}
